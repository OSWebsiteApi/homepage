import React, { FC } from "react";

interface componentHeader extends React.HTMLAttributes<HTMLHeadElement> {

}
/*
const NewHeader: FC<componentHeader> = ({ title }: { title: string}) => { 
    return( <h1>{title}</h1>
    )
}

const NewHeaderWithContext: FC<componentHeader> = ({ children }: { children: string}) => { 
    return( <h1>{children}</h1>
    )
}


export default NewHeader 
*/
import React from "react";
import Layout from "../Layout/Layout";

const Login = () => {
    return (
        <main>
            <h1>Login</h1>
            <Layout />
        </main>
    )
};

export default Login;